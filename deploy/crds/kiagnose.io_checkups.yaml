---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: checkups.kiagnose.io
spec:
  group: kiagnose.io
  names:
    kind: Checkup
    listKind: CheckupList
    plural: checkups
    shortNames:
    - ckup
    singular: checkup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Status
      jsonPath: .status.conditions[?(@.status=="True")].type
      name: Status
      type: string
    - description: Reason
      jsonPath: .status.conditions[?(@.status=="True")].reason
      name: Reason
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Checkup is the Schema for the kiagnose checkups API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CheckupSpec defines a checkup to run
            properties:
              clusterRoleNames:
                items:
                  type: string
                type: array
              data:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              image:
                type: string
              roleNames:
                items:
                  type: string
                type: array
              timeout:
                type: string
            required:
            - image
            - timeout
            type: object
          status:
            description: CheckupStatus defines the observed state of Checkup
            properties:
              conditions:
                items:
                  properties:
                    lastHearbeatTime:
                      format: date-time
                      type: string
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
